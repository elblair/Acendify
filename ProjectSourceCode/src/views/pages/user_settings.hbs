<div class="container mt-5">
  <div class="card">
    <div class="card-body">
      <h1 class="card-title mb-4">{{username}}'s Profile</h1>
     
      <div class="mb-3">
        <strong>Member Since:</strong> 
        <span class="text-muted">{{created_at}}</span>
      </div>
    </div>
  </div>
</div>